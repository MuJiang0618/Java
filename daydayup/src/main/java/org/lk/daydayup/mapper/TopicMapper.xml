<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.lk.daydayup.mapper.TopicMapper">
<!--    用于搜索topic-->
    <select id="getTopicsByIds" resultType="Topic">
        select * from topic where id in
        <foreach collection="list" index="index" item="item" open="(" separator="," close=")">
            #{item}
        </foreach>
    </select>

    <select id="getTopicById" resultType="Topic">
        select * from topic where id = #{topicId}
    </select>

    <select id="getItemsByTopicId" resultType="Item">
        select * from item where topic_id = #{topicId}
    </select>

    <insert id="addTopic" parameterType="Topic" useGeneratedKeys="true" keyProperty="topicId">
        insert into topic (topic_name, like, dislike, hot)
            values (#{topicName}, ${0}, ${0}, ${0})
    </insert>

</mapper>